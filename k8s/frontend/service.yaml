apiVersion: v1
kind: Service
metadata:
  name: shopping-app-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
spec:
  type: LoadBalancer
  selector:
    frontendapp: shoppingwebapp
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
